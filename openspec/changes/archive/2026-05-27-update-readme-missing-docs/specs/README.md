# Specs — Update README Missing Docs

This change is a **documentation-only update**. No new capabilities or modified requirements exist — the README already accurately describes the project's behavior. The changes are limited to:

1. Adding `apps/mobile-kmp/` to the monorepo structure and apps table
2. Adding `PRD-UX.md` to the Documentation section
3. Adding missing `dev:backend` and `dev:web-pwa` scripts to root `package.json`
4. Fixing SDK description

No spec files are needed for this change.
